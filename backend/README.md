# Legacy Express API

The HTTP API now lives in **Next.js Route Handlers** under `src/app/api/`.

This `backend/` folder is kept for reference and optional local use:

```bash
npm run dev:legacy-api
```

Prefer `npm run dev` (Next only) for day-to-day work and **Vercel** deploys.
